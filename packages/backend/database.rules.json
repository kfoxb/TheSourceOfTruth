{
  "rules": {
    "tasks": {
      ".write": "auth.token.author === true || auth.token.editor === true || auth.token.approver === true"
    },
    "users": {
      "$userId": {
        ".read": "$userId === auth.uid",
        ".write": false
      }
    },
    "eng": {
      ".read": "auth != null",
      "documents": {
        ".read": "auth != null",
        "create": {
          ".read": "auth != null",
          "$doc": {
            ".write": "auth.token.author === true && data.child('locked').val() !== true"
          }
        },
        "edit": {
          ".read": "auth != null",
          "$doc": {
            ".write": "!data.exists() && auth.token.author === true"
          }
        },
        "primary": {
          ".read": "auth != null",
          ".write": "(!data.child('phase').exists() || data.child('phase').val() === 'create') && auth.token.author === true"
        }
      }
    }
  }
}
