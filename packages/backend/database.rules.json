{
  "rules": {
    "tasks": {
      ".write": "auth.token.author === true || auth.token.editor === true || auth.token.approver === true"
    },
    "users": {
      "$userId": {
        ".read": "$userId === auth.uid",
        ".write": false
      }
    },
    "eng": {
      ".read": "auth != null",
      ".write": "auth != null",
      "documents": {
        ".read": "auth!= null",
        ".write": "auth.token.author === true || auth.token.editor === true || auth.token.approver === true"
      }
    }
  }
}
