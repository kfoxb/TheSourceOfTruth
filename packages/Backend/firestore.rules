service cloud.firestore {
  match /databases/{database}/documents {
    match /{document=**} {
      allow read;
    }
    match /journals/{journal} {
      allow create, update: if request.auth.token.author == true;
    }
  }
}
