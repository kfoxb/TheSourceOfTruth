{
 "devDependencies": {
  "lerna": "2.9.1"
 },
 "private": true,
 "scripts": {
  "build": "lerna exec -- yarn build",
  "bootstrap": "lerna bootstrap",
  "clean": "lerna clean",
  "lint": "lerna exec -- yarn lint",
  "test": "lerna exec -- yarn test",
  "validate": "lerna exec -- yarn validate"
 },
 "workspaces": ["packages/*"]
}
