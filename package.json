{
	"devDependencies": {
		"lerna": "2.5.1"
	},
  "scripts": {
    "build": "lerna exec -- yarn build",
    "bootstrap": "lerna bootstrap && lerna exec -- yarn bootstrap",
    "clean": "lerna clean && lerna exec -- yarn clean",
    "lint": "lerna exec -- yarn lint",
    "test": "lerna exec -- yarn test",
    "validate": "lerna exec -- yarn validate"
  }
}
